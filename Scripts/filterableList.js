import { recipes } from "./recipes.js";

/*let filterInput = document.querySelector(".label-input");
filterInput.addEventListener("change", filterIngredients);

function filterIngredients() {
  let filterValue = document.querySelector(".label-input").value.toLowerCase();
  console.log(filterValue);
  let searchText = document.querySelector(".search-container_bl");
  let list = document.querySelectorAll(".filter-content");
  for (let q = 0; q < list.length; q++) {
    let a = list[q].getElementsByTagName("a");
    if (searchText.innerHTML.toLowerCase().indexOf(filterValue) > -1) {
      list[q].style.display = "block";
    }
  }
}

if (word && word !== "") {
  if (
    recipes.appliance.toLowerCase().includes(word.toLowerCase()) &&
    ingredients.findIndex(
      (i) => recipes.appliance.toLowerCase() == i.toLowerCase()
    ) < 0
  ) {
    recipeIngredient += `<li class="filter-content">${recipes.appliance}</li>`;
    appliance.push(recipes.appliance);
    console.log(ingredient.ingredient);
  }
} else {
  if (recipeIngredient.includes(ingredient.ingredient) === false) {
    recipeIngredient += `<li class="filter-content">${recipes.appliance}</li>`;
    appliance.push(recipes.appliance);
  }
}





if (word && word !== "") {
    if (
      recipes.appliance.toLowerCase().includes(word.toLowerCase()) &&
      appliance.findIndex(
        (i) => recipes.appliance.toLowerCase() == i.toLowerCase()
      ) < 0
    ) {
      recipeAppliance += `<li class="filter-content">${recipes.appliance}</li>`;
      appliance.push(recipes.appliance);
    }
  } else {
    if (recipeAppliance.includes(recipes.appliance) === false) {
      recipeAppliance += `<li class="filter-content">${recipes.appliance}</li>`;
      appliance.push(recipes.appliance);
    }
*/
